<Window
  x:Class="RayTracing.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="MainWindow"
  Width="1600"
  Height="900"
  ResizeMode="NoResize"
  mc:Ignorable="d">
  <Window.Resources>
    <ResourceDictionary>
      <SolidColorBrush x:Key="CanvasColor" Color="{StaticResource DarkGray}" />
      <Style x:Key="MainCanvas" TargetType="Canvas">
        <Setter Property="Width" Value="1600" />
        <Setter Property="Height" Value="900" />
        <Setter Property="Background" Value="{StaticResource CanvasColor}" />
      </Style>
    </ResourceDictionary>
  </Window.Resources>

  <Window.Content>
    <Canvas x:Name="MainCanvas" Style="{StaticResource MainCanvas}" />
  </Window.Content>

</Window>
